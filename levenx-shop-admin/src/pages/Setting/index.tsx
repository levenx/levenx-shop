import React, { Component } from 'react';
import { withMenus } from 'src/utils';

class Setting extends Component {
    render() {
        return (
            <div>
Setting
            </div>
        );
    }
}

export default withMenus(Setting);