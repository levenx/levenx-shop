import React, { Component } from 'react';
import { withMenus } from 'src/utils';

class ContentConfig extends Component {
    render() {
        return (
            <div>
                ContentConfig
            </div>
        );
    }
}

export default withMenus(ContentConfig);